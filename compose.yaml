version: '3.7'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: symfony_app
    environment:
      - DATABASE_URL=postgresql://postgres:vTrDLmISQPajxoWRTiWPOykaMDbqGgzl@caboose.proxy.rlwy.net:14363/railway
    volumes:
      - .:/var/www/html
    ports:
      - "80:80"
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
