version: '3.7'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: symfony_app
    environment:
      - APP_ENV=prod
      - PORT=8080   # Optionnel si tu veux explicitement définir le port pour ton app
    volumes:
      - .:/var/www/html
    ports:
      - "8080:8080"  # Utilise un port fixe (8080)
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
